<!-- #TODO make a template for this sentence -->
<p>
  Within <%= Goal.days[@goal.days] %> I will <%= @goal.text %> which will be <%= Goal.values[@goal.value] %>  and <%= Goal.efforts[@goal.effort] %>.
</p>

<ul>
	<li>
		<%= link_to 'Edit', edit_goal_path(@goal) %>
	</li>
	<li>
		<%= link_to 'Delete', goal_path(@goal), method: :delete, data: { confirm: 'Are you sure?' } %>
	</li>
</ul>
